[project]
name = "deepsequence-recommender"
version = "1.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.coverage.run]
source = ["app", "src"]
omit = ["tests/*"]
